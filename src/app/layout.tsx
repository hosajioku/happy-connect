import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "@/components/layout/Providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Happy Connect – Connecting Serious Singles for Meaningful Relationships",
  description:
    "Happy Connect is a serious relationship and matchmaking platform connecting real, verified singles for meaningful relationships and marriage.",
  keywords: ["dating", "matchmaking", "relationship", "marriage", "singles", "Nigeria"],
  openGraph: {
    title: "Happy Connect – Find Your Perfect Match",
    description: "Connecting serious singles for meaningful relationships and marriage.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
